// WiFi credentials — copy this file to wifi_credentials.h and fill in your values.
// wifi_credentials.h is .gitignored and will not be committed.

#pragma once

#define WIFI_SSID     "your-ssid"
#define WIFI_PASSWORD "your-password"
